apiVersion: apps/v1
kind: Deployment
metadata:
  name: DEPLOYMENT_NAME
  namespace: NAMESPACE
  labels:
    app: PROJECT
spec:
  selector:
    matchLabels:
      project: PROJECT
  template:
    metadata:
      namespace: NAMESPACE
      labels:
        project: PROJECT
    spec:
      containers:
         - name: PROJECT
           image: REGISTRY_HOSTNAME/PROJECT:TAG_VERSION
           securityContext:
             allowPrivilegeEscalation: false
             runAsNonRoot: true      # опционально, но хорошая практика
             runAsUser: 1000         # опционально
           ports:
             - containerPort: PORT_CONTAINER
           env:
             - name: MICROSERVICE_POSTS_HOST
               valueFrom:
                 secretKeyRef:
                   name: posts-microservice-production-config-secret
                   key: MICROSERVICE_POSTS_HOST
             - name: MICROSERVICE_POSTS_PORT
               valueFrom:
                 secretKeyRef:
                   name: posts-microservice-production-config-secret
                   key: MICROSERVICE_POSTS_PORT
             - name: CORS_ALLOWED_ORIGINS
               valueFrom:
                 secretKeyRef:
                   name: posts-microservice-production-config-secret
                   key: CORS_ALLOWED_ORIGINS
             - name: POSTGRES_POSTS_URL
               valueFrom:
                 secretKeyRef:
                   name: posts-microservice-production-config-secret
                   key: POSTGRES_POSTS_URL
             - name: RABBITMQ_URI
               valueFrom:
                 secretKeyRef:
                   name: posts-microservice-production-config-secret
                   key: RABBITMQ_URI
             - name: NODE_ENV
               valueFrom:
                 secretKeyRef:
                   name: posts-microservice-production-config-secret
                   key: NODE_ENV
